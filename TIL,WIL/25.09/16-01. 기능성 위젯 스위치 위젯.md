
switch 위젯 
```dart
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  var switchState = false; // Switch 위젯의 on/off 상태를 저장하는 역할
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        body: Center(
          child: Switch(
            value: switchState,
            onChanged: (bool newValue) {
              //onChanged : setState()로 state를 newValue로 바꾸면 -> flutter가 build를 다시 실행 -> UI가 업데이트 됨
              // 토글 이벤트 처리
              setState(() {
                switchState = newValue;
              });
            },
          ),
        ),
      ),
    );
  }
}

```

- switch의 상태에 따라 UI가 업데이트 되므로 statefulWidget 사용

---

slide 위젯
```dart
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  var slideState = 50.0; //newValue가 double타입이므로 똑같이 double타입의 값을 입력
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        body: Center(
          child: Slider(
            value: slideState,
            min: 0,
            max: 100,
            onChanged: (double newValue) {
              // 슬라이더 변경값에 따른 이벤트 처리
              setState(() {
                slideState = newValue;
              });
            },
          ),
        ),
      ),
    );
  }
}

```